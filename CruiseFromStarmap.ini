; Cruise From Starmap 0.1.0
;
; Highlight a planet, moon, or indexed station in the Starmap and use the
; separate Cruise action. Current-system targets can be marked or entered by
; hold; eligible remote targets use vanilla JUMP THEN CRUISE routing.
;
; Put personal changes in CruiseFromStarmapCustom.ini beside this file. The
; custom file wins and is not replaced by updates.

[General]
; Per-selection, input-transition, course, and lifecycle detail in the SFSE log.
bVerboseLog=true
